# ForensicLens Tauri Native Shell Placeholder

This folder is reserved for future Tauri (Rust + Webview) desktop integration.

When building for Windows Desktop in Phase 7:
1. Initialize Tauri with `npm run tauri init`.
2. Implement native OS filesystem bindings in `src/services/filesystem/` for streaming large disk images directly from disk.
